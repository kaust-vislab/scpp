# Copyright (c) BBP/EPFL 2011-2014 Stefan.Eilemann@epfl.ch
# All rights reserved. Do not distribute without further notice.

# Never mind the [] sections. They are only here for doxygen.
# Remove them in your project.

# //! [Setup]
cmake_minimum_required(VERSION 2.8 FATAL_ERROR)
project(Hello)
list(APPEND CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/CMake
  ${CMAKE_SOURCE_DIR}/CMake/common)
include(GitExternal)
# //! [Setup]

# //! [Versioning]
set(VERSION_MAJOR "0")
set(VERSION_MINOR "1")
set(VERSION_PATCH "0")
set(VERSION_ABI 1)
# //! [Versioning]

# //! [Scripts]
set(HELLO_DESCRIPTION "A sample sclang client in cpp, for SuperCollider")
set(COMMON_PROJECT_DOMAIN vis.kaust.edu.sa)
include(Common)
include(FindPackages) # Generated by Buildyard and checked in
# //! [Scripts]

# //! [Targets]
add_subdirectory(hello)
add_subdirectory(apps)
add_subdirectory(tests)
# //! [Targets]

# //! [Footer]
include(CPackConfig)
include(DoxygenRule)
# //! [Footer]
